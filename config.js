const config = {
    db: {
        // should not be exposed
          
        host: "db4free.net",
        user: "root@localhost",
        password: "root",
        database: "TestDb",
      },
      listPerPage: 10,
    };
    module.exports = config;
